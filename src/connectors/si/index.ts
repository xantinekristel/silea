// SI Retrieval App connector (Phase 5). Only the mock is available in the
// browser build. A live adapter would be added once IT confirms the app
// accepts scripted/batch input (SPEC §10, question 6).
export { mockSiConnector } from "./mockSiConnector";
export type { SiConnector } from "../types";
